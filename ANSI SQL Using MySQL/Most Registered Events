-- Most Registered Events 
-- List top 3 events based on the total number of user registrations. 

use cognizant;

select event_id, count(user_id) as user_registeration_count
from registrations
group by event_id
order by user_registeration_count desc limit 3;
